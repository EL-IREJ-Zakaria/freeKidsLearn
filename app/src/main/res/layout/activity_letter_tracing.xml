<?xml version="1.0" encoding="utf-8"?>
<!-- ========================================================================
     ACTIVITY_LETTER_TRACING.XML - Écran de traçage moderne pour enfants
     ========================================================================
     L'écran principal d'apprentissage où l'enfant:
     - Voit la lettre en très grand format
     - Écoute la prononciation
     - Trace avec son doigt sur le canvas

     Design ludique avec grands boutons colorés et feedback visuel.
     ======================================================================== -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/background_light">

    <!-- ════════════════════════════════════════════════════════════════
         SECTION 1: Toolbar moderne
         ════════════════════════════════════════════════════════════════ -->
    <com.google.android.material.card.MaterialCardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:cardCornerRadius="0dp"
        app:cardElevation="@dimen/toolbar_elevation"
        app:cardBackgroundColor="@color/primary">

        <com.google.android.material.appbar.MaterialToolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="@dimen/toolbar_height"
            android:background="@android:color/transparent"
            app:titleTextColor="@color/white"
            app:titleTextAppearance="@style/TextAppearance.Material3.TitleLarge"
            app:navigationIconTint="@color/white"
            app:navigationIcon="@drawable/ic_arrow_back"
            app:title="@string/trace_letter" />

    </com.google.android.material.card.MaterialCardView>

    <!-- ════════════════════════════════════════════════════════════════
         SECTION 3: Zone de dessin (Canvas) - Style ardoise moderne
         ════════════════════════════════════════════════════════════════ -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:paddingHorizontal="@dimen/spacing_medium"
        android:paddingBottom="@dimen/spacing_small">

        <com.google.android.material.card.MaterialCardView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            app:cardCornerRadius="@dimen/card_corner_radius_large"
            app:cardElevation="@dimen/card_elevation_high"
            app:cardBackgroundColor="@color/white"
            app:strokeWidth="3dp"
            app:strokeColor="@color/primary_light">

            <!-- Zone de dessin personnalisée -->
            <com.example.freekidslearn.ui.DrawingView
                android:id="@+id/drawingView"
                android:layout_width="match_parent"
                android:layout_height="match_parent" />

        </com.google.android.material.card.MaterialCardView>

        <!-- Icône crayon décorative -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="top|end"
            android:layout_margin="@dimen/spacing_medium"
            android:text="✏️"
            android:textSize="@dimen/text_size_xlarge" />

    </FrameLayout>

    <!-- ════════════════════════════════════════════════════════════════
         SECTION 4: Boutons de contrôle - Design moderne et coloré
         ════════════════════════════════════════════════════════════════ -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="@dimen/spacing_medium"
        android:gravity="center">

        <!-- Bouton Précédent - Style outline -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/buttonPrevious"
            android:layout_width="0dp"
            android:layout_height="@dimen/button_height"
            android:layout_weight="1"
            android:layout_marginEnd="@dimen/spacing_small"
            android:text="◀ Préc."
            android:textSize="@dimen/text_size_medium"
            android:textColor="@color/primary"
            app:cornerRadius="@dimen/button_corner_radius"
            app:strokeWidth="2dp"
            app:strokeColor="@color/primary"
            style="@style/Widget.Material3.Button.OutlinedButton" />

        <!-- Bouton Effacer - Style accent (orange) -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/buttonClear"
            android:layout_width="0dp"
            android:layout_height="@dimen/button_height"
            android:layout_weight="1"
            android:layout_marginHorizontal="@dimen/spacing_small"
            android:text="🗑️ Effacer"
            android:textSize="@dimen/text_size_medium"
            android:textColor="@color/white"
            app:backgroundTint="@color/accent"
            app:cornerRadius="@dimen/button_corner_radius"
            app:elevation="@dimen/card_elevation"
            style="@style/Widget.Material3.Button" />

        <!-- Bouton Suivant - Style filled primary -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/buttonNext"
            android:layout_width="0dp"
            android:layout_height="@dimen/button_height"
            android:layout_weight="1"
            android:layout_marginStart="@dimen/spacing_small"
            android:text="Suiv. ▶"
            android:textSize="@dimen/text_size_medium"
            android:textColor="@color/white"
            app:backgroundTint="@color/primary"
            app:cornerRadius="@dimen/button_corner_radius"
            app:elevation="@dimen/card_elevation"
            style="@style/Widget.Material3.Button" />

    </LinearLayout>

</LinearLayout>
