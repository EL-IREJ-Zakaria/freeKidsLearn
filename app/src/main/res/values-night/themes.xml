<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- ====================================================================
         THEMES.XML (NIGHT) - Thème sombre pour application enfants
         ====================================================================
         Note: Pour une application enfant, on garde les couleurs vives
         même en mode sombre pour maintenir l'aspect ludique.
         ==================================================================== -->

    <style name="Base.Theme.FreeKidsLearn" parent="Theme.Material3.DayNight.NoActionBar">

        <!-- Couleurs primaires - Garder le bleu vif même en mode sombre -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorPrimaryContainer">@color/primary_light</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Couleurs secondaires/accent -->
        <item name="colorSecondary">@color/accent</item>
        <item name="colorSecondaryContainer">@color/accent_light</item>
        <item name="colorOnSecondary">@color/white</item>

        <!-- Fond légèrement plus sombre mais pas noir (mieux pour les yeux des enfants) -->
        <item name="colorSurface">@color/card_background</item>
        <item name="colorOnSurface">@color/text_primary</item>
        <item name="android:colorBackground">@color/background_light</item>
        <item name="colorOnBackground">@color/text_primary</item>

        <!-- Barre de statut -->
        <item name="android:statusBarColor">@color/primary_dark</item>
        <item name="android:navigationBarColor">@color/background_light</item>

    </style>
</resources>
